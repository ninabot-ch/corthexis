mcp>=1.2          # works on both 1.x (FastMCP) and 2.x (MCPServer)
pyyaml>=6.0
# Needed by the `http` and `openai` embedding backends only.
httpx>=0.27
# Needed by the default `local` embedding backend only.
sentence-transformers>=3.0
